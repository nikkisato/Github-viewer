import React from 'react';
import PropTypes from 'prop-types';
import './Header.css';

const Header = () => {
  return (
    <>
      <header>
        <h1>Github Viewer</h1>
      </header>
    </>
  );
};

// Header.propTypes = {
// };

export default Header;
